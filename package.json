{
  "name": "aframe-markdown",
  "version": "0.1.0",
  "description": "aframe-markdown renders markdown using SDF text.",
  "license": "MIT",
  "repository": "github:brianpeiris/aframe-markdown",
  "main": "aframe-markdown.js",
  "files": [
    "aframe-markdown.js",
    "fonts/**"
  ],
  "scripts": {
    "start": "browser-sync start -s -w --no-open --no-notify --no-ghost-ui"
  },
  "peerDependencies": {
    "aframe": "^0.8.2"
  },
  "dependencies": {
    "marked": "^0.5.2"
  },
  "devDependencies": {
    "browser-sync": "^2.26.3"
  }
}
